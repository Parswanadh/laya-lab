# LEDGER.md — verified claims

One row per claim. A claim is **promoted** only when it has: a `REPRODUCED` verdict from a
verifier who did not produce it, **and** a cross-verifier that attacked it and failed to
falsify it. Until then it stays `candidate`.

**Status vocabulary** — `candidate` (measured, unverified) · `reproduced` (independent
re-run matched) · `falsified` (attacked successfully; claim is wrong) · `unresolved`
(could not be tested on this hardware, with reason) · `refuted-by-us` (our own hypothesis
failed; recorded as a negative result)

| # | claim | evidence | kind | verifier | status |
|---|---|---|---|---|---|
| L-001 | Upstream ships `max_len=1024`, `head_max_len=256` for `laya-multilingual` | `multilingual/rl_agent_config.json` @ `55cf4c4` | config read | — | candidate |
| L-002 | *(superseded in framing by L-016/L-018; the code reading stands)* `build_sequence` keeps the **head** of a long state for str/dict input; `truncate_left=True` only for list state | `laya/common.py:126`, `laya/agent.py:571` | source read | — | candidate |
| L-003 | `laya-multilingual` encoder = mmBERT-base, 22 layers, **8 global** (idx 0,3,6,9,12,15,18,21), 14 sliding, hidden 768, `max_position_embeddings=8192`, RoPE 160000 both | HF `multilingual/encoder/config.json` @ `55cf4c4` | config read | — | candidate |
| L-004 | Sliding half-window = `local_attention // 2` = **64** tokens | `laya/fast.py:53-55`, `local_attention=128` | source read | — | candidate |
| L-005 | ⚠ **Now understood differently — see L-016.** Upstream measured 0.35 flat accuracy for pad ≥ 2000 at limit=1024; 0.85–0.90 at limit=8192; latency 0.016→3.507 s (n=20/cell) | `research/results/long_context_multilingual.json` | upstream artifact | not re-run here | candidate |
| L-006 | PR #363 `predict_long` is open and occupies the windowed-scan lane; its default window still misroutes the billing tail | upstream PR #363 body | upstream artifact | — | candidate |
| L-007 | Issue #99 (long noisy multilingual ≈ chance) is open; maintainer calls long-doc encoders "a strong direction we are investigating" | upstream issue #99 | upstream artifact | — | candidate |
| L-008 | **Dynamic NTK RoPE scaling is the identity for any `seq_len <= max_position_embeddings`** — provable from `modeling_rope_utils.py`, which clamps `seq_len` up to `max_position_embeddings` first. `linear`/`yarn`/`longrope` have no such protection and rescale in-window positions unconditionally. Our config declares `rope_type: "default"` for both layer types. | HF source + R-001 §1.1; https://github.com/huggingface/transformers/blob/main/src/transformers/modeling_rope_utils.py | source read | research (self) | candidate |
| L-009 | YaRN / NTK / LongRoPE cost **7.56 / 4.34 / 3.52 MMLU points** when extending Phi3-mini to 128k; YaRN −15.2%, NTK −9.3% overall, GSM8K −21.15 / −14.55 abs. LongRoPE2's fix is to **switch rescaled RoPE off** for in-window inputs. | arXiv 2502.20082 | paper-asserted | research (self) | candidate |
| L-010 | On **Inverted EURLEX** (decisive section moved to the end): BERT truncate@512 **70.53**, BERT+random-chunk-select **71.47**, BERT+TextRank **71.30**, ToBERT 67.31, CogLTX 70.80, **Longformer@4096 56.47**. The sparse long-context encoder lost by ~14 points to truncation; chunk selectors were best. | arXiv 2203.11258 (ACL'22), Table 2 | third-party eval | research (self) | candidate |
| L-011 | MIMIC-III window ablation (each row **retrained**): local window 32→512 bought **+0.4 micro-F1** at 2.5× test cost. Bound on window-widening headroom. | arXiv 2204.06683 (EMNLP Findings'22) | paper-asserted | research (self) | candidate |
| L-012 | **No published sliding-window or global/local pattern ablation exists for mmBERT.** Full-text search of arXiv 2509.06888 finds the values only in a static hyperparameter table. Our H2/H3 measurements are novel for this model family. | arXiv 2509.06888; R-001 §1.9, §7 gap 4 | literature gap | research (self) | candidate |
| L-013 | **No published "position of decisive evidence vs accuracy" study exists for a bidirectional encoder with a decision head.** *Lost in the Middle* and both *Found in the Middle* papers are decoder-only. | R-001 §7 gap 5 | literature gap | research (self) | candidate |
| L-014 | ModernBERT Appendix D: global-every-layer "yielded identical downstream performance" to global-every-3rd. **Caveat: ablation sequence length unstated** (main pretraining was 1024), so this is *not* evidence about 8192. | arXiv 2412.13663 App. D | paper-asserted | research (self) | candidate |
| L-015 | **V-001: both orchestrator artifacts REPRODUCED exactly.** orch-baseline 10/10 cells (19,7,7,7,7,19,18,16,12,9 of 20); orch-diagnostic 13/13 cells; per-item 0/20 predictions differ, max abs delta p_gold = 0.000000. Zero nondeterminism across repeated in-process and cross-process runs. Tolerance: exact for counts, 1e-4 for probabilities. | `findings/V-001.md`, `experiments/V-001/` | independent verification | verifier (not the producer) | **reproduced** |
| L-016 | **REFUTES our "0.35 long-context floor".** The four `limit=1024, pad>=1000` rows share ONE retained-state hash across all 80 rows and that state is provably the filler document **with the request removed**. Zero request tokens are in the model input. The model answers `technical` 20/20; 0.35 is the `technical` share (7/20) — **below the trivial constant-billing baseline of 0.45**. It is a truncation/no-evidence result, **not** a long-context result. | V-001 defect #7; state hashes in `experiments/V-001/` | independent verification | verifier | **falsified** |
| L-017 | **REFUTES our "our 8192 arm degrades far below upstream's" claim as confounded.** `orch-baseline` differs from upstream in >=5 ways: JSON wrapper (+5 tokens) vs plain string; upstream prepends `"\n\nActual request: "` which we omit; whole-repeat vs exact padding; no warm-up; cuda/bf16 vs mps/fp32. | V-001 defect #8 | independent verification | verifier | **falsified** |
| L-018 | Real prefix length is **45 head tokens (markers at 12/20/29/39) + 5-token JSON wrapper = 50**, not 256. `head_max_len` is an upper bound. Shipped state budget at `max_len=1024` is **978 tokens, not ~764**. | M-001; V-001 defect #9 | derivation + verification | math + verifier | **reproduced** |
| L-019 | **Reachability graph has diameter 1.** Layer 0 is `full_attention`, so the set reachable from a marker after one hop is `[0,n)`. Min layers for a nonzero path from any state token to any marker = **1, independent of distance**. Refutes the "8 cross-document hops" framing. | M-001 (exact simulation, n=8192) | derivation | math | **falsified (our claim)** |
| L-020 | Cross-document edge budget at n=8192: 24,015,296 edges, **99.9% from the 8 global layers**, 0.1% from the 14 sliding; the sliding contribution is **constant** at 29,120 edges and does not grow with n. | M-001 | derivation (modelled) | math | candidate |
| L-021 | FLOPs(8192)/FLOPs(1024) = **35.5x [MODELLED]**, encoder-only 32.4x. Algebraic bound: the ratio is increasing in the quadratic coefficient with supremum 64x, so **no layer pattern or width can make this model predict 219x**. The model **over-predicts** the normalized measurement by 2.4-10x growing with n. **Use for rank/direction only, never an absolute ratio.** | M-001 | modelled + miss reported | math | candidate |
| L-022 | **H3 and H5 are NOT powered at n=400** for a +0.05 effect: exact unconditional McNemar power is 0.89/0.62/0.48 at q=0.10/0.20/0.30. A null on a +0.05 arm at n=400 must be reported as **UNDERPOWERED**, not as no effect. | M-001 | power analysis | math | candidate |
| L-023 | **E-001: the collapse reproduces on a balanced pool, n=20/cell.** pad 0: 0.95/0.95; 1000: 0.35/0.70; 2000: 0.35/0.70; 4000: 0.30/0.55; 7000: 0.35/0.40 (limit 1024/8192). At 1024 every padded cell answers `technical` 20/20 with **zero request tokens surviving** truncation (head=45, window=978). At 8192 the balanced pool recovers only to **0.55-0.70**, not upstream's 0.85-0.90, with the full request present. | `experiments/baseline-repro/`, `findings/E-001.md` | measured | engineer | candidate |
| L-024 | **P-a REFUTED — window widening does not help.** No dose-response. The +1-item hint at n=20 (p=1.0) **reverses to −19 items (p=0.0094)** on the balanced n=120 set; the *narrowing* ablation (64) moves pad=7000 by the same +1 item, so the n=20 signal was noise. Widening to 1024 **collapses** pad=4000: 0.600 → 0.150 (p=0.012). | `findings/E-002.md` §2–3 | measured, paired, liveness-proved | engineer (issue #4) | **falsified (our hypothesis)** |
| L-025 | **P-b REFUTED, both halves — all-global is harmful and is not quadratic.** Accuracy *falls* at every length ≥1000: **0.000 at pad=1000**, 0.333 at pad=7000 = the majority-class rate (p=7.5e-9), with all 120 items predicted `technical`. Cost is **+4–6% median latency**, not a multiple; VRAM unchanged at 2068 MiB. | `findings/E-002.md` §2–3, §5 | measured, paired | engineer (issue #4) | **falsified (our hypothesis)** |
| L-026 | **Liveness proved, with a falsifying control.** Observed per-layer max attended distance after patching: baseline `{64: 14, 7068: 8}`; all-global `{n: 22}`. `allglobal_w512_combo` is **bit-identical to `allglobal`** (20/20, max abs delta p_gold = 0.000000), which falsifies "a sliding mask is still in effect after the P-b patch". | `findings/E-002.md` §1.2–1.3 | measured | engineer | **reproduced** |
| L-027 | **The baseline reproduces upstream EXACTLY on upstream's own texts.** n=20, RTX 4070 CUDA bf16: `max_len=1024` → 0.95/0.65/0.35/0.35/0.35 and `max_len=8192` → 0.95/0.80/0.85/0.95/0.40, vs upstream published 0.95/0.65/0.35/0.35/0.35 and 0.95/0.80/0.85/0.90/0.40. Every default-max_len cell exact; 8192 matches 4/5 exactly, the fifth one item apart. Token-identical to upstream's data, 160/160 `input_tokens`. | `findings/E-001.md`, `experiments/baseline-repro*/` | measured | engineer (issue #1) | **reproduced** |
| L-028 | **Plateau is pool-dependent.** Upstream's texts reach 0.80–0.95 at 8192; the balanced 9-language pool reaches only **0.55–0.70** with the full request present. The lower ceiling is the evaluation set, not the machine or bf16. **Thresholds must be set from the matched pool, never from upstream's 0.90.** | `findings/E-001.md` | measured | engineer | candidate |
| L-029 | **`pad=7000 @ max_len=8192` is the dilution cell.** Both budgets give **0.40** with the whole document in view; paired 1024-vs-8192 has **1 discordant pair, p=1**. Truncation is excluded by construction there, so it is the one cell where an aggregation-level intervention can show an effect. | `findings/E-001.md` | measured, paired | engineer | candidate |
| L-030 | **The 1024 collapse is a cliff, not a gradient, and it is truncation direction.** Position sweep at pad=4000: needle at head 0.75; needle at 0.25/0.50/0.75/1.00 → 0.35/0.35/0.35/0.30 with modal share 1.00; paired head-vs-tail **0 wrong→right / 9 right→wrong, p=0.0039**. | `findings/E-001.md` | measured, paired | engineer | candidate |
| L-031 | **Powered baseline, n=200/cell, balanced pool** (majority = position-only oracle = **0.335**, random 0.250): pad 0 → **0.840** [0.785,0.890] both budgets; pad 1000 → **0.325** [0.260,0.390] vs **0.705** [0.640,0.765]; pad 2000 → 0.335 vs 0.670; pad 4000 → 0.330 vs **0.495** [0.425,0.565]; pad 7000 → 0.335 vs **0.420** [0.355,0.490]. Paired McNemar 1024-vs-8192: p = 3.8e-18 / 2.2e-18 / 1.0e-08 / 7.6e-05 — **all four survive Holm–Bonferroni**. | `experiments/baseline-power-n200/`, `findings/E-001.md` §5.5 | measured, powered, paired | engineer | **reproduced** |
| L-032 | **Length effect AT 8192 with everything visible** (paired): 1000→2000 p=0.435 (flat); **2000→4000 p=1.2e-06 (real drop)**; 4000→7000 p=0.032 (does not survive Holm). Dilution is isolated from truncation in the 2000→4000 transition. | `findings/E-001.md` §5.5 | measured, paired | engineer | candidate |
| L-033 | **`pad=7000 @ 8192` = 0.420 is the truncation-free primary cell** (`request_tokens_kept = 1.000`). It is where an aggregation-level intervention can show an effect, and its oracle is 0.335. | `findings/E-001.md` §7 | measured, powered | engineer | candidate |

> L-005 is an **upstream** number, not ours. It is recorded here as the stated problem and
> must be reproduced on this machine before any of our deltas are quoted against it.
